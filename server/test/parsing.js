const assert = require('assert');


describe('first test', () => {

	it('first test is passed', () => {
		assert(true === true);
	});

	it('another test for travis', () => {
		assert(1 === 2);
	});

});
